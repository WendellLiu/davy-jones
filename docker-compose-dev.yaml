version: '3.7'
services:
  davy-jones-dev:
    build:
      context: ./
      dockerfile: Dockerfile-dev
    env_file: ./.env
    entrypoint: cargo watch -x "run --features 'production'"
    ports:
      - "8000:8000"
    volumes:
      - ./:/app/
